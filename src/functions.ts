function makeOrder(order: string): string | null {
    if (!order) return null
    return order
}


function createTea(order: {
    type: string,
    sugar: number,
    size: "small" | "large"
}): {} {
    return order
}


